package abalonegame;

public class HumanPlayer extends Player {

    /**
     * Creates a new instance of HumanPlayer
     */
    public HumanPlayer(GameFrame Parent) {
        super(Parent);
    }

    public void DoStep() { // Function DoStep is empty as the Human is the one who does the move.
    }
}
